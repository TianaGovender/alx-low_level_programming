#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"

/**
 * _strlen - gives length of string
 * @s: char
 * Return: the length of the string
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);

	return (len);
}
