#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "main.h"

/**
 * _abs - prints absolute integer
 * @n: an integer
 *
 * Return: absolute integer
 */
int _abs(int n)
{
	int c = abs(n);

	return (c);
}
